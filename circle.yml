machine:
  services:
    - docker

test:
  override:
      - docker info


deployment:
  hub:
    branch: develop
    commands:
      - docker build -t coinpit/apache2:develop .
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push coinpit/apache2