machine:
  services:
    - docker

test:
  override:
      - docker info


deployment:
  hub:
    branch: cryptin
    commands:
      - docker build -t coinpit/apache2:cryptinpr .
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push coinpit/apache2:cryptinpr